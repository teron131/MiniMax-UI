{
  "name": "minimax-ui",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "ui:dev": "npm --prefix frontend run dev:frontend",
    "ui:build": "npm --prefix frontend run build",
    "backend:dev": "uv run python -m backend.app",
    "backend:test": "uv run pytest",
    "backend:lint": "uv run ruff check .",
    "backend:format": "uv run black ."
  },
  "workspaces": ["frontend"]
} 